{
  "version": "0.0.2",
  "description": "Twitter Links Chrome/Firefox extension",
  "author": "James Knight",
  "license": "MIT",
  "scripts": {
    "build": "run-s clean build:extensions",
    "build:extensions": "run-p build:chrome build:firefox",
    "build:chrome": "cd src/chrome; zip -r ../../extension_chrome.zip *; cd ../../",
    "build:firefox": "cd src/firefox; zip -r ../../extension_firefox.zip *; cd ../../",
    "clean": "rm -rf *.zip"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
